import { DynamicTranslationDirective } from './dynamic-translation.directive';

describe('DynamicTranslationDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicTranslationDirective();
    expect(directive).toBeTruthy();
  });
});
