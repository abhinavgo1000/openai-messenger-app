import { SiteSearchDirective } from './site-search.directive';

describe('SiteSearchDirective', () => {
  it('should create an instance', () => {
    const directive = new SiteSearchDirective();
    expect(directive).toBeTruthy();
  });
});
